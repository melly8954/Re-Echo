spring:
  application:
    name: re-echo-be

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    encoding: UTF-8
    clean-disabled: true

management:
  endpoints:
    web:
      exposure:
        include: health,info