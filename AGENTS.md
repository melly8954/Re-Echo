# AGENTS.md

이 문서는 Re-Echo 프로젝트에서 AI 에이전트가 작업할 때 따라야 하는
프로젝트 로컬 규칙이다.

전역 `AGENTS.md`보다 이 문서의 프로젝트 규칙을 우선 적용한다.
단, 이 문서에 없는 일반 개발 원칙은 전역 `AGENTS.md`를 따른다.

# 1. 프로젝트 기준 문서 우선순위

작업 전 `docs/` 폴더의 Markdown 문서를 확인한다.
문서 간 충돌이 있으면 임의로 판단하지 말고 충돌 내용을 설명한 뒤
사용자 확인을 받는다.

우선순위는 다음과 같다.

1. `docs/prd.md`
2. `docs/Architecture.md`
3. `docs/ERD.md`
4. `docs/API.md`
5. `docs/coding-convention.md`
6. `docs/decisions.md`
7. 프로젝트 `AGENTS.md`
8. 전역 `AGENTS.md`

문서별 역할은 다음과 같이 구분한다.

- 제품 목표, MVP 범위, 사용자 정책은 `docs/prd.md`를 따른다.
- 시스템 구조와 기술 선택은 `docs/Architecture.md`를 따른다.
- 데이터 구조, 상태값, 관계, 인덱스 전략은 `docs/ERD.md`를 따른다.
- Endpoint, Request, Response, Error Code, 인증 방식, 실시간 이벤트
  계약은 `docs/API.md`를 따른다.
- 코드 구조, 네이밍, 계층 책임, 예외 처리, 테스트, Git 규칙은
  `docs/coding-convention.md`를 따른다.
- 이미 확정된 설계 결정의 이유와 영향은 `docs/decisions.md`를 따른다.

# 2. 작업 기본 원칙

- 요구사항을 충분히 이해한 뒤 작업한다.
- 불명확한 요구사항은 임의로 추측하지 않는다.
- 문서와 코드에서 확인하지 못한 내용은 확인하지 못했다고 말한다.
- 요청되지 않은 기능, API, 테이블, 컬럼, 설정은 추가하지 않는다.
- 기존 문서의 범위와 의도를 유지한다.
- 이 프로젝트는 프로덕션으로 간주하고 임시 우회 코드를 작성하지 않는다.
- 오류 수정은 원인 분석, 수정, 영향 범위 확인, 검증 순서로 진행한다.
- 새 기술이나 새 구조를 도입해야 할 경우 먼저 이유와 영향 범위를
  설명하고 사용자 확인을 받는다.

# 3. 구현 범위 제한

- PRD에 없는 기능을 추가하지 않는다.
- Architecture에 없는 기술, 배포 구조, 런타임 구성 요소를 임의로
  도입하지 않는다.
- ERD에 없는 테이블, 컬럼, 관계, 상태값을 임의로 추가하지 않는다.
- API 문서에 없는 Endpoint, Request, Response, Error Code를 임의로
  추가하지 않는다.
- API 구현은 `docs/API.md`의 공통 응답 envelope, `contents` 목록 필드,
  Error Code prefix, WebSocket event envelope을 기준으로 한다.
- 인증/인가 정책을 임의로 완화하지 않는다.
- WebSocket 이벤트는 문서에 정의된 메시지와 입력 중 표시 범위를
  임의로 넘기지 않는다.
- 파일 처리는 Presigned URL 기반 직접 전송과 DB 메타데이터 관리
  방식을 따른다.
- Redis는 최종 진실 소스가 아니라 보조 상태와 실시간 전파 계층으로만
  사용한다.

# 4. 최소 변경 및 파일 보존 규칙

- 사용자가 요청한 작업 범위와 직접 관련된 파일만 수정한다.
- 관련 없는 리팩터링, 포맷팅, 파일 이동을 하지 않는다.
- 사용자가 명시적으로 요청하지 않은 한 기존 파일을 삭제 후
  재생성하지 않는다.
- 사용자가 명시적으로 요청하지 않은 한 기존 파일 전체를 덮어쓰지
  않는다.
- 기존 문서의 섹션 순서, 네이밍, 스타일을 최대한 보존한다.
- 대규모 재작성이나 구조 변경이 필요하면 작업을 중단하고 이유와
  영향 범위를 설명한 뒤 사용자 확인을 받는다.
- 자동 포맷팅으로 무관한 diff를 대량 발생시키지 않는다.
- 삭제가 실패했거나 거부된 작업을 전체 덮어쓰기로 우회하지 않는다.
- 임시 디버그 로그나 확인용 코드는 작업 완료 전 제거한다.

# 5. 검증 및 작업 완료 기준

현재 저장소 루트에서 확인된 테스트, 빌드, 린트 실행 파일이나 설정은
없다.

검증 기준은 다음과 같다.

- 검증 명령은 프로젝트에 실제로 존재하는 명령만 사용한다.
- 새 실행 명령이 생기기 전까지는 문서 검토, diff 확인, Markdown 구조
  확인을 기본 검증으로 수행한다.
- 검증하지 못한 부분은 이유를 명확히 설명한다.
- 변경한 파일과 변경 이유를 요약한다.
- 남은 위험이나 사용자가 확인해야 할 부분을 적는다.
- 작업 완료 후 커밋 메시지를 제안한다.

커밋 메시지는 다음 형식을 사용한다.

```text
type: 변경 내용
```

사용 가능한 type은 다음과 같다.

```text
feat, fix, refactor, docs, style, test, perf, chore, ci, build
```

문서 작업의 기본 type은 `docs`를 사용한다.

커밋 제안, staging, commit 작업을 수행할 때는
`docs/coding-convention.md`의 커밋 분리 원칙을 따른다.
사용자가 구현, 수정, 반영을 요청한 작업은 검증 후 커밋까지 수행하되,
push는 사용자가 직접 수행하므로 자동으로 실행하지 않는다.

# 6. 부칙

1. AI 처리 과정 메시지와 최종 응답은 한국어로 작성한다.
2. 처리 과정 메시지를 제외한 모든 사용자 응답에는 존댓말을 사용한다.
3. 한글 문서와 응답은 UTF-8 기준으로 읽고 작성한다.
4. 한글 인코딩 깨짐이나 타 언어 혼입이 보이면 즉시 한국어 문장으로
   정정한다.
5. 주석을 작성해야 할 경우 한국어로 작성하고, 구현 의도와 설계 이유를
   설명한다.
6. 단순 사용법보다 왜 그렇게 구현하는지를 우선 설명한다.
7. 최신 정보가 필요한 문제는 가능한 경우 공식 문서나 웹 검색으로
   확인한다.
8. 웹 검색이나 외부 확인이 불가능하면 확인하지 못한 부분을 명확히
   남긴다.
